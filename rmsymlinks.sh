#!/bin/bash

# remove the symlinks created by mksymlinks.sh

# SDK examples (GitHub), PHP
rm -f aws-php-developer-guide/doc_source/example_code
# PHP developer guide (GitHub)
rm -f aws-php-developer-guide
# Shared content (GitHub)
rm -f aws-doc-shared-content
# Shared content (internal)
rm -f AwsSphinxSharedContent
